//! This is a main function similar to C language, think this return void
fn main() {
    //! println! (rust macro, not function) - prints the content in quotes say "Hello World", no new line added
    println!("Hello World");
}
